apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    run: hello-world
  name: hello-world
spec:
  replicas: 1
  selector:
    matchLabels:
      run: hello-world
  strategy: {}
  template:
    metadata:
      labels:
        run: hello-world
    spec:
      containers:
      - image: gcr.io/gcp-learning-237423/hello-gcp
        name: hello-world
        resources: {}
status: {}
